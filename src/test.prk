//d = dict([(date, data) for date, data in read_dailies(finput)])

/*
function test0(n) {
    val l = [0,1,2,3]
    val d = {"test": 2, "piet": 4}

    for(x in l) {
        for(y in range(4)) {
            println(x + y)
        }
    }

    println(d)

    for(a in d) {
        println(a)
    }

}

function test2() {
    val l = ["a", "b", "c", "d"]
    for(i in range(4)) {
        println(l[i])
    }
}

function test3() {
    val c = 30
    //function literal:
    val sum = function(a,b) {
        return a + b + c
    }
    println(sum(10, 20))
}
*/

function test4() {
    val a = 10
    val b = 20
    return {
        "sum": function() {
            return a + b
        }
    }
}


function main(a, b) {
    val d = test4()
    println(d["sum"]())
}


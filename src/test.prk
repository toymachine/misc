function fac(n) {
    if(n == 0) {
        println("n == 0")
        println("piet kaap aa")
        1
    }
    else {
        n * fac(n - 1)
    }
}

function minus(a, b) {
    return a - b + fac(5)
}
